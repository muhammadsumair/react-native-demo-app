import { StyleSheet } from 'react-native';

export const LoginStyles = StyleSheet.create({
    container: {
        padding: 20,
        marginTop: 50
    },
    title: {
        fontSize: 27
    },
    errMsg: {
        color: 'red',
        textAlign: 'center',
        paddingVertical: 20
    },
    submitContainer: {
        marginTop: 20
    }
});